git config --global user.name "Твое Имя"
git config --global user.email "твоя_почта@mail.ru"
git init
git remote add origin ССЫЛКА_НА_ТВОЙ_ЛОКАЛЬНЫЙ_ГИТ
git add .
git commit -m "Готовый проект"
git push -u origin master
